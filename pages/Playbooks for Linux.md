## 🧯 Playbook: сервис не работает / 502 / “не отвечает”
collapsed:: true
	- **Цель:** понять, жив ли сервис, почему упал, слушает ли порт
	- **Шаги**
		- Проверить статус юнита
			- ```
			  systemctl status <service> --no-pager
			  ```
		- Посмотреть логи сервиса (с текущей загрузки)
			- ```
			  journalctl -u <service> -b --no-pager | tail -n 200
			  ```
		- Перезапустить и снова проверить
			- ```
			  sudo systemctl restart <service>
			  systemctl status <service> --no-pager
			  ```
		- Проверить, слушает ли порт
			- ```
			  ss -tulpen | grep -E ':(80|443|<порт>)\b'
			  ```
		- Проверить конфиг (если применимо)
			- nginx:
				- ```
				  sudo nginx -t
				  ```
			- apache:
				- ```
				  sudo apachectl configtest
				  ```
	- **Типичные причины**
		- неверный конфиг, права на файлы, порт занят, закончился диск/память, зависимость (DB) недоступна
		  
		  ---
- ## 💾 Playbook: закончилось место на диске (No space left on device)
  collapsed:: true
	- **Цель:** быстро найти “кто съел” и освободить безопасно
	- **Шаги**
		- Где закончилось
			- ```
			  df -hT
			  ```
		- Кто съел место (по верхним директориям)
			- ```
			  sudo du -h --max-depth=1 / | sort -h | tail -n 20
			  sudo du -h --max-depth=1 /var | sort -h | tail -n 20
			  ```
		- Найти большие файлы
			- ```
			  sudo find /var -type f -size +200M -printf '%p %s\n' 2>/dev/null | sort -n | tail -n 30
			  ```
		- journald: посмотреть объём и подчистить (осторожно)
			- ```
			  journalctl --disk-usage
			  sudo journalctl --vacuum-time=7d
			  ```
		- Если Docker (осторожно!)
			- ```
			  docker system df
			  docker image prune
			  docker container prune
			  docker volume prune
			  ```
	- **Частые виновники**
		- `/var/log`, `/var/lib/docker`, кэши `/var/cache`, дампы/бэкапы
		  
		  ---
- ## 🔥 Playbook: высокая нагрузка / сервер тормозит
  collapsed:: true
	- **Цель:** понять CPU vs RAM vs Disk I/O
	- **Шаги**
		- Общая картина
			- ```
			  uptime
			  top
			  ```
		- Кто грузит CPU/память
			- ```
			  ps aux --sort=-%cpu | head -n 15
			  ps aux --sort=-%mem | head -n 15
			  ```
		- Проверить процесс по PID
			- ```
			  ps -fp <PID>
			  ```
		- Если подозрение на Disk I/O
			- (если установлен sysstat)
			- ```
			  iostat -xz 1 5
			  ```
			- Быстро посмотреть ошибки диска/ядра
			- ```
			  dmesg -T | tail -n 100
			  ```
		- Аккуратно остановить процесс
			- ```
			  kill -TERM <PID>
			  sleep 3
			  kill -KILL <PID>   # крайний случай
			  ```
	- **Заметки**
		- Высокий load при низком CPU часто = I/O wait или “зомби” на диске/сети
		  
		  ---
- ## 🧠 Playbook: не хватает памяти / OOM killer
  collapsed:: true
	- **Цель:** подтвердить OOM и найти виновника
	- **Шаги**
		- Сколько памяти/свопа
			- ```
			  free -h
			  ```
		- ТОП по памяти
			- ```
			  ps aux --sort=-%mem | head -n 20
			  ```
		- Был ли OOM
			- ```
			  dmesg -T | grep -i -E "oom|killed process" | tail -n 50
			  ```
		- Логи конкретного сервиса
			- ```
			  journalctl -u <service> -b --no-pager | tail -n 200
			  ```
	- **Дальше по ситуации**
		- лимиты памяти (systemd/cgroups), оптимизация, добавление RAM/swap (взвешенно)
		  
		  ---
- ## 🌐 Playbook: “нет интернета” / сеть не работает
  collapsed:: true
	- **Цель:** отделить проблему IP/маршрута/DNS
	- **Шаги**
		- Есть ли IP
			- ```
			  ip a
			  ```
		- Есть ли default route
			- ```
			  ip r
			  ```
		- Работает ли “голый IP”
			- ```
			  ping -c 3 1.1.1.1
			  ```
		- Если IP работает, но домены нет — DNS
			- ```
			  cat /etc/resolv.conf
			  dig google.com +short
			  ```
		- Проверка HTTP
			- ```
			  curl -I https://example.com
			  ```
		- Где отваливается маршрут
			- ```
			  traceroute 1.1.1.1
			  ```
	- **Типичные причины**
		- нет маршрута, сломан DNS, MTU/VPN, firewall, проблемы у провайдера
		  
		  ---
- ## 🚪 Playbook: порт закрыт / не могу подключиться (ssh/http/db)
  collapsed:: true
	- **Цель:** понять “не слушает” vs “блокирует firewall” vs “не маршрутизируется”
	- **Шаги**
		- На сервере: слушает ли порт
			- ```
			  ss -tulpen | grep -E ':(22|80|443|<порт>)\b'
			  ```
		- С клиента: проверка TCP коннекта
			- ```
			  nc -vz <host> <port>
			  ```
		- Проверить firewall (что установлено)
			- nftables:
				- ```
				  sudo nft list ruleset | less
				  ```
			- ufw:
				- ```
				  sudo ufw status verbose
				  ```
		- Если это SSH: логи
			- ```
			  sudo journalctl -u ssh -b --no-pager | tail -n 200
			  ```
			  
			  ---
- ## 🔒 Playbook: Permission denied (права/владелец/группа)
  collapsed:: true
	- **Цель:** быстро понять, что именно не так с правами
	- **Шаги**
		- Проверить права/владельца
			- ```
			  ls -lah /path/to/file_or_dir
			  ```
		- Под каким пользователем работает сервис
			- ```
			  ps aux | grep <service>
			  ```
		- Исправить владельца (если нужно)
			- ```
			  sudo chown -R user:group /path/to/dir
			  ```
		- Базовые права (типовой безопасный минимум)
			- директория:
				- ```
				  sudo chmod 755 /path/to/dir
				  ```
			- файл:
				- ```
				  sudo chmod 644 /path/to/file
				  ```
		- Проверить группы пользователя
			- ```
			  id <user>
			  ```
	- **Анти-паттерны**
		- `chmod 777` “чтобы заработало” — почти всегда плохая идея
		  
		  ---
- ## 📦 Playbook: apt сломан / зависимости / dpkg прерван
  collapsed:: true
	- **Цель:** восстановить пакетный менеджер
	- **Шаги**
		- Обновить индексы
			- ```
			  sudo apt update
			  ```
		- Починить зависимости
			- ```
			  sudo apt -f install
			  ```
		- Доделать конфигурацию пакетов
			- ```
			  sudo dpkg --configure -a
			  ```
		- Посмотреть версии/репозитории
			- ```
			  apt-cache policy <pkg>
			  ```
		- Логи apt
			- ```
			  tail -n 200 /var/log/apt/term.log
			  ```
			  
			  ---
- ## 🧾 Playbook: быстро посмотреть логи “прямо сейчас”
  collapsed:: true
	- **Цель:** оперативно увидеть причину
	- **Шаги**
		- Логи сервиса с “follow”
			- ```
			  journalctl -u <service> -f
			  ```
		- Последние ошибки системы
			- ```
			  journalctl -p err -b --no-pager | tail -n 200
			  ```
		- Если логи в файле
			- ```
			  tail -n 200 /var/log/<file>.log
			  tail -f /var/log/<file>.log
			  ```
- ## 🐳 Playbook: Docker Compose сервис не стартует / “unhealthy” / перезапуски
  collapsed:: true
	- **Цель:** быстро понять, что ломается (логика, сеть, env, volumes)
	- **Шаги**
		- Перейти в папку проекта и проверить состав
			- ```
			  docker compose ps
			  docker compose config
			  ```
		- Посмотреть логи (последние 200 строк)
			- ```
			  docker compose logs --tail=200 -f
			  ```
		- Проверить healthcheck (если есть)
			- ```
			  docker compose ps
			  docker inspect <container> --format '{{json .State.Health}}' | jq
			  ```
			- (если jq нет — просто `docker inspect <container> | less`)
		- Перезапуск конкретного сервиса
			- ```
			  docker compose restart <service>
			  ```
		- Полный пересозданный старт (часто решает env/volume issues)
			- ```
			  docker compose up -d --force-recreate
			  ```
		- Если подозрение на порты/конфликт
			- ```
			  ss -tulpen | grep -E ':(80|443|<port>)\b'
			  docker compose ps
			  ```
	- **Типичные причины**
		- не поднялась зависимость (db/redis), неправильный `.env`, права на volume, занят порт, миграции не прошли
		  
		  ---
- ## 🐳 Playbook: Docker “съел диск” / /var/lib/docker растёт
  collapsed:: true
	- **Цель:** безопасно понять, что занимает место, и почистить
	- **Шаги**
		- Сколько занято docker-ом
			- ```
			  docker system df
			  ```
		- Освободить “мусор” (осторожно)
			- ```
			  docker image prune
			  docker container prune
			  docker network prune
			  docker volume prune
			  ```
		- Агрессивная чистка (только если понимаешь последствия)
			- ```
			  docker system prune -a
			  ```
	- **Заметки**
		- Volume prune может снести данные, если ты держал их только в volume и контейнеров уже нет — будь внимателен
		  
		  ---
- ## 🐳 Playbook: контейнер “не видит сеть” / DNS внутри контейнера не работает
  collapsed:: true
	- **Цель:** отличить проблему хоста, docker bridge, DNS, firewall
	- **Шаги**
		- Проверить DNS на хосте
			- ```
			  ping -c 2 1.1.1.1
			  dig google.com +short
			  ```
		- Проверить внутри контейнера
			- ```
			  docker exec -it <container> sh
			  # внутри:
			  ping -c 2 1.1.1.1
			  nslookup google.com
			  ```
		- Посмотреть сети docker
			- ```
			  docker network ls
			  docker network inspect <network> | less
			  ```
		- Проверить, не блокирует ли firewall/NAT
			- ```
			  sudo nft list ruleset | less
			  ```
	- **Типичные причины**
		- сломан resolv.conf на хосте, кастомный DNS в docker, конфликт подсетей, правила firewall
		  
		  ---
- ## 🔐 Playbook: WireGuard туннель “поднялся”, но трафик не ходит
  collapsed:: true
	- **Цель:** быстро найти “рукопожатие есть?” и где ломается маршрут/NAT
	- **Шаги**
		- Проверить статус и handshake
			- ```
			  sudo wg show
			  ```
			- Смотри `latest handshake`, `transfer`
		- Проверить интерфейс и маршруты
			- ```
			  ip a
			  ip r
			  ```
		- Проверить IP forwarding (если это роутер/сервер)
			- ```
			  sysctl net.ipv4.ip_forward
			  ```
		- Проверить NAT/firewall (если клиентам нужен интернет через сервер)
			- nftables пример (ориентир, зависит от твоей схемы):
			- ```
			  sudo nft list ruleset | less
			  ```
		- Проверить AllowedIPs (частая причина!)
			- На клиенте: чтобы шёл нужный трафик (например, `0.0.0.0/0` для full-tunnel или подсети для split)
			- На сервере: чтобы знал, куда отдавать подсети клиента
	- **Типичные причины**
		- неправильные AllowedIPs, нет маршрута, нет NAT, MTU, DNS не настроен
		  
		  ---
- ## 🔐 Playbook: WireGuard + “интернет есть, но сайты не открываются” (DNS)
  collapsed:: true
	- **Цель:** если ping по IP работает, но домены — нет
	- **Шаги**
		- Проверить: IP есть?
			- ```
			  ping -c 2 1.1.1.1
			  ```
		- Проверить: DNS?
			- ```
			  nslookup google.com
			  ```
		- Проверить, какой DNS задан (на клиенте)
			- WireGuard config: `DNS = ...`
			- Система:
				- ```
				  cat /etc/resolv.conf
				  ```
		- Если у тебя AdGuard Home — проверить, доступен ли он из туннеля (порт/маршруты)
		  
		  ---
- ## 📈 Playbook: Prometheus “Target DOWN”
  collapsed:: true
	- **Цель:** понять — exporter мёртв? сеть? firewall? wrong port/path?
	- **Шаги**
		- Быстро проверить с Prometheus-хоста доступность exporter
			- ```
			  curl -sS http://<target>:<port>/metrics | head
			  ```
		- Проверить порт на target-хосте
			- ```
			  ss -tulpen | grep -E ':<port>\b'
			  ```
		- Проверить systemd-сервис exporter (если systemd)
			- ```
			  systemctl status <exporter> --no-pager
			  journalctl -u <exporter> -b --no-pager | tail -n 200
			  ```
		- Проверить firewall
			- ```
			  sudo nft list ruleset | less
			  ```
		- Проверить конфиг Prometheus (job/labels/port)
			- ```
			  grep -n "job_name\|<target>\|<port>" /etc/prometheus/prometheus.yml
			  ```
	- **Типичные причины**
		- exporter не запущен, слушает на localhost, порт закрыт, неверный адрес/порт/путь, TLS/BasicAuth
		  
		  ---
- ## 📊 Playbook: Grafana “No data” / панель пустая
  collapsed:: true
	- **Цель:** отделить проблему данных (Prometheus/Loki) от проблемы запроса/времени
	- **Шаги**
		- Проверить time range (частая банальность)
			- Поставить “Last 1h / 6h”, включить авто-обновление
		- Проверить Data Source “Save & Test” в UI
		- Проверить запрос напрямую в источнике
			- Prometheus: открыть UI Prometheus и выполнить тот же metric/query
			- Loki: открыть Explore → выполнить log query
		- Проверить метки/label
			- Часто меняются: `instance`, `job`, `hostname`, `container`, `app`
	- **Типичные причины**
		- неправильный time range, datasource не доступен, метка изменилась, alert/recording rules не грузятся
		  
		  ---
- ## 🪵 Playbook: Loki / Promtail “логи не приходят”
  collapsed:: true
	- **Цель:** понять — promtail не читает? не пушит? Loki не принимает?
	- **Шаги**
		- Проверить, жив ли promtail
			- ```
			  systemctl status promtail --no-pager
			  journalctl -u promtail -b --no-pager | tail -n 200
			  ```
		- Проверить, что файл логов реально растёт
			- ```
			  tail -n 50 /var/log/<file>.log
			  ```
		- Проверить конфиг promtail (пути, labels)
			- ```
			  sudo cat /etc/promtail/config.yml | less
			  ```
		- Проверить доступность Loki endpoint
			- ```
			  curl -sS http://<loki>:3100/ready
			  ```
	- **Типичные причины**
		- неправильный путь к логам, нет прав читать, logrotate сменил файл, promtail не видит новые lines, сеть/URL Loki неправильный
		  
		  ---
- ## 🧱 Playbook: Proxmox VM/LXC не стартует
  collapsed:: true
	- **Цель:** быстро увидеть конкретную ошибку (storage, lock, config, kernel)
	- **Шаги**
		- Проверить статус
			- ```
			  qm status <VMID>
			  pct status <CTID>
			  ```
		- Попробовать старт из CLI (даёт понятную ошибку)
			- VM:
				- ```
				  qm start <VMID>
				  ```
			- LXC:
				- ```
				  pct start <CTID>
				  ```
		- Посмотреть логи
			- VM:
				- ```
				  journalctl -b --no-pager | tail -n 300
				  ```
			- LXC:
				- ```
				  journalctl -b --no-pager | tail -n 300
				  ```
				- (иногда полезно)
				- ```
				  cat /var/log/pve/tasks/active | tail
				  ```
		- Проверить storage/место
			- ```
			  pvesm status
			  df -h
			  ```
		- Проверить “lock”
			- ```
			  qm unlock <VMID>
			  pct unlock <CTID>
			  ```
	- **Типичные причины**
		- закончилось место на storage, битый диск, зависший lock, неправильные права на storage, проблемы с network bridge
		  
		  ---
- ## 🌉 Playbook: Proxmox сеть — VM есть, а интернета нет
  collapsed:: true
	- **Цель:** понять: bridge/маршрут/DNS/firewall внутри VM
	- **Шаги**
		- На Proxmox: проверить bridge и IP
			- ```
			  ip a
			  ip r
			  ```
		- На Proxmox: проверить, что VM подключена к правильному bridge
			- ```
			  qm config <VMID> | grep -i net
			  ```
			- или в UI: Hardware → Network Device → Bridge
		- Внутри VM: есть ли IP/route/DNS
			- ```
			  ip a
			  ip r
			  cat /etc/resolv.conf
			  ping -c 2 1.1.1.1
			  ping -c 2 google.com
			  ```
		- Проверить firewall в Proxmox (если включён на Datacenter/Node/VM)
	- **Типичные причины**
		- неверный bridge (vmbr0/vmbr1), нет default route, DNS пустой, включён PVE firewall, VLAN mismatch
		  
		  ---
- ## 🔁 Playbook: Proxmox “бэкапы не идут / vzdump падает”
  collapsed:: true
	- **Цель:** быстро понять — место, права, storage, lock, скорость диска
	- **Шаги**
		- Проверить место на backup storage
			- ```
			  pvesm status
			  df -h
			  ```
		- Посмотреть задачи/логи backup
			- ```
			  ls -1 /var/log/pve/tasks/ | tail
			  ```
			- (и смотреть конкретный лог из UI или по task id)
		- Проверить, не “locked” ли VM/CT
			- ```
			  qm status <VMID>
			  pct status <CTID>
			  ```
		- Прогнать backup вручную (если знаешь команду/путь storage)
			- ```
			  vzdump <VMID> --storage <storage> --mode snapshot
			  ```
			  
			  ---
- ## 🧩 Playbook: AdGuard Home / DNS-сервис “интернет есть, но DNS не резолвит”
  collapsed:: true
	- **Цель:** отделить “AdGuard умер” от “клиенты не используют DNS”
	- **Шаги**
		- Проверить доступность DNS-сервера с клиента
			- ```
			  nslookup google.com <DNS_IP>
			  ```
		- Проверить, что клиенты реально используют этот DNS
			- Windows: `ipconfig /all`
			- Linux:
				- ```
				  cat /etc/resolv.conf
				  ```
		- Проверить, что порт 53 слушается на DNS-хосте
			- ```
			  ss -tulpen | grep -E ':53\b'
			  ```
		- Проверить логи/сервис AdGuard (если systemd/docker)
			- systemd:
				- ```
				  systemctl status adguardhome --no-pager
				  journalctl -u adguardhome -b --no-pager | tail -n 200
				  ```
			- docker:
				- ```
				  docker logs --tail=200 -f <container>
				  ```
	- **Типичные причины**
		- поменялся DNS в DHCP, firewall блокирует 53, AdGuard не стартует, конфликт с systemd-resolved
		  
		  ---